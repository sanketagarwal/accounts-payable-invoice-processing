# Local fixtures need no external credentials.
INVOICE_READER=fixture
INVOICE_READER_MODEL=openai/gpt-5.6-sol
OPENAI_API_KEY=
INVOICE_ROOT=.
INVOICE_MAX_BYTES=20000000
# MASTRA_DB_URL=file:/absolute/path/to/mastra.db
ACCOUNTING_PROVIDER=fixture

# QuickBooks sandbox (only when ACCOUNTING_PROVIDER=quickbooks)
QBO_REALM_ID=
QBO_ACCESS_TOKEN=
QBO_BASE_URL=https://sandbox-quickbooks.api.intuit.com
# Explicit demo-only fallback; replace with a real standalone screener in production.
SANCTIONS_SCREENING=

# Intuit QuickBooks MCP (only when ACCOUNTING_PROVIDER=quickbooks-mcp)
QBO_MCP_SERVER_PATH=
QBO_MCP_TOKEN_STORE_PATH=
